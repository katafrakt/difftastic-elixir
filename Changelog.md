## 0.2.0
* Added `difft_assert_against_file` ExUnit assertion to make it possible to compare dynamic content with already existing file, without creating a temporary file for the latter.
* Added basic snapshot testing capabilities via `Difftastic.SnapshotTest` module.
